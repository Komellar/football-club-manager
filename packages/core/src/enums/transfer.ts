export enum TransferStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
}

export enum TransferType {
  SIGNING = "signing",
  LOAN = "loan",
  LOAN_RETURN = "loan_return",
  SALE = "sale",
  RELEASE = "release",
  RETIREMENT = "retirement",
}
