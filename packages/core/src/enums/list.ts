export enum CompareOperator {
  EQ = "_eq",
  GT = "_gt",
  LT = "_lt",
  GTE = "_gte",
  LTE = "_lte",
  NE = "_ne",
  BETWEEN = "_between",
}

export enum FilterMode {
  PARTIAL = "partial",
  EXACT = "exact",
  GT = "gt",
  LT = "lt",
  GTE = "gte",
  LTE = "lte",
  NE = "ne",
  BETWEEN = "between",
}

export enum SortOrder {
  ASC = "ASC",
  DESC = "DESC",
}
