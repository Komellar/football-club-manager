export enum ContractType {
  PERMANENT = "permanent",
  LOAN = "loan",
  TRIAL = "trial",
  YOUTH = "youth",
  PROFESSIONAL = "professional",
}

export enum ContractStatus {
  ACTIVE = "active",
  EXPIRED = "expired",
  TERMINATED = "terminated",
  PENDING = "pending",
}
