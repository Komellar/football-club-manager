export enum ContractType {
  PERMANENT = "permanent",
  LOAN = "loan",
  YOUTH = "youth",
}

export enum ContractStatus {
  ACTIVE = "active",
  EXPIRED = "expired",
  TERMINATED = "terminated",
  PENDING = "pending",
}
